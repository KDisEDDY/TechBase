package algorithm;

/**
 * LC 670
 * 修改 数字中的任意两位从而达到整个数最大
 *
 */
public class ChangeToBiggest {

    int changeToBiggest(int value) {
        char[] valueArray = String.valueOf(value).toCharArray();

        return 0;
    }
}
